services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    restart: unless-stopped
    volumes:
      - ${KUMA_DIRECTORY}:/app/data
    ports:
      # <Host Port>:<Container Port>
      - "3001:3001"
